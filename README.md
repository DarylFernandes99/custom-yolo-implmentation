# custom-yolo-implmentation

[Dataset link](https://cocodataset.org/#download) 


## Create conda environment
`Note: Make sure you have conda installed on your system before running the following commands and update the environment.yml file prefix to your conda env folder.`
```bash
conda env create -f environment.yml
conda activate hpc_project
```
